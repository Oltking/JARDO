# Cost-Accuracy Router table (spec §5.2) — hot-reloadable, edits apply live.
# Model choices pending eval validation (QUESTIONS.md Q4).

daily_budget_usd = 2.0        # Fireworks daily cap (QUESTIONS.md Q1 suggestion)
est_output_tokens = 512

# AMD GPU droplet (scale-to-zero: empty endpoint = not running = not routable).
# Set to "http://<droplet-ip>:8000/v1" while a vLLM droplet is up.
vllm_endpoint = ""
vllm_hourly_usd = 1.99        # MI300X rate, docs/vendor/amd/developer-cloud-getting-started.md
vllm_tokens_per_hour = 3600000

[tiers]
ollama_local = "llama3.2:3b"
vllm_large = ""               # set when a vLLM droplet is provisioned (Phase 2b)
fireworks_cheap = "fireworks/gpt-oss-20b"
fireworks_mid = "fireworks/minimax-m2p7"
fireworks_quality = "fireworks/kimi-k2p6"
